# Design Debt Register — LoyerTracker

| ID | Constat | Criticité | Responsable | Échéance | Preuve attendue | Statut |
| --- | --- | --- | --- | --- | --- | --- |
| DD-611-01 | UXR-001 projet non renseigné | Majeur | UX/UI Design Lead | Prochain lot Frontend significatif | UXR-001 revu et lié au lot | **Préparé** — instancié pour US-125 (2026-07-30) puis étendu en base de recherche produit (socle EP-17, 2026-07-30) ; revue humaine par un UX/UI Design Lead désigné toujours requise avant clôture |
| DD-611-02 | DDS-001/DSG-001 et inventaire composants non instanciés | Majeur | Design Architect | Avant Gate 04A du prochain lot UI | DDS, DSG, inventaire et décision Gate | **En traitement** — **validation Design Architect rendue le 2026-07-31** (`DSG-001.md` §Avis de validation), **acceptée par le Product Owner (jptshilombo@gmail.com), 2026-07-31, sans réserve** comme validation humaine du contenu (contenu vérifié par recoupement direct avec le code — composants, lignes, couleurs, `data-testid` — tous exacts, une inexactitude corrigée : §Fondations, `VerifyReceiptComponent` hors du patron radius/breakpoint, nouvelle dette `DD-EP17-09`) ; **non close** : conformément au Validation Framework CGPA v6.1.1 §5, la clôture d'une dette « Majeur » exige une preuve d'implémentation — aucun composant `lt-*` codé, CODE INTERDIT en vigueur. L'acceptation Product Owner lève uniquement le sous-bloqueur Gate 04A « non validé humainement » (`gate-04A-decision-ep17-lot0.md` §4), pas la dette elle-même |
| DD-611-03 | Traçabilité Story-écran-composant-test incomplète | Majeur | Frontend Architect | Avant développement du prochain lot UI | matrice de traçabilité approuvée | **En traitement** — **validation Frontend Architect rendue le 2026-07-31** (`traceability-ui-loyertracker.md` §Avis de validation), **acceptée par le Product Owner (jptshilombo@gmail.com), 2026-07-31, sans réserve** comme validation humaine du contenu (neuf affirmations factuelles recoupées avec le code réel, toutes exactes, structure et règle « À définir » respectées sans exception) ; **non close** : les preuves de test attendues (unitaire/a11y/responsive/Visual Review) restent structurellement inexistantes, aucun composant `lt-*`/`Notifications*` codé, CODE INTERDIT en vigueur. L'acceptation Product Owner lève uniquement le sous-bloqueur Gate 04A « matrice approuvée » (`gate-04A-decision-ep17-lot0.md` §4), pas la dette elle-même |
| DD-611-04 | Régression visuelle non industrialisée | Mineur | Design QA | Avant prochain Gate Staging UI selon risque | rapport Visual Review ou exemption approuvée | Ouvert — inchangé |

## Nouvelles dettes identifiées (cadrage socle UI PrimeNG/Keycloak, 2026-07-30)

| ID | Constat | Criticité | Responsable | Échéance | Preuve attendue | Statut |
| --- | --- | --- | --- | --- | --- | --- |
| DD-EP17-01 | Aucun thème Keycloak personnalisé : continuité visuelle Angular/IdP inexistante (login/mot de passe oublié/erreurs au thème par défaut) | Majeur | Design Architect, Security Architect Keycloak | Avant Lot 4 du Plan d'Exécution | Thème livré en Staging isolé + Gate Staging dédié | Ouvert |
| DD-EP17-02 | Aucun état « accès refusé »/404 uniforme côté client (403 serveur non traduit, fallback `**` silencieux vers `/bailleur`) | Majeur | Frontend Architect | Avant migration des écrans concernés | `lt-error-state` implémenté et testé | Ouvert |
| DD-EP17-03 | Aucune source de tokens partagée entre Angular et Keycloak ; choix Option A (JSON) vs Option B (CSS commun) non tranché formellement | Majeur | Design Architect | Avant Lot 4 du Plan d'Exécution | Décision tracée (DDS dédiée si nécessaire) + implémentation | **Décision prise (2026-08-02)** — Option B (source CSS commune) confirmée par le Product Owner (« je valide ta recommandation », en réponse à l'instruction Gate 04A/02A Lot 4, `gate-04A-decision-ep17-lot4.md`), conforme à la recommandation déjà documentée par `ADR-UI-001` §Isolation entre Angular et Keycloak. **Non close** — aucune implémentation (`tokens.css` commun n'existe pas encore), conformément au Validation Framework CGPA v6.1.1 §5 (une dette n'est close que par la preuve attendue effectivement produite) |
| DD-EP17-04 | Hétérogénéité de composants : le patron `.panel`/`.panel-head`/`.toolbar`/`.list`/`.row` est dupliqué à l'identique dans au moins 4 composants (`AlertesListeComponent`, `AuditJournalComponent`, deux dashboards) | Mineur | Frontend Architect | Lot 2 du Plan d'Exécution | `lt-data-table`/`lt-section-card` livrés et adoptés | **Partiellement traité (2026-08-02)** — `lt-data-table` livré (`US-132`), adopté pour la liste des Biens (`US-133`/`US-134`, patron `.list`/`.row`) puis pour la liste des Patrimoines (même jour, patron `.item` — hétérogène de `.list`/`.row` mais même dette de fond : liste ad hoc dupliquée) du dashboard Bailleur ; statut rendu via `lt-status-tag`. Note : la liste des Patrimoines n'a **pas** reçu la sélection au clic/clavier (`selectable`) contrairement à Biens — la sélection d'un patrimoine à modifier reste pilotée par le sélecteur `<select>` existant, migration de présentation strictement, aucune interaction nouvelle ajoutée. Reste Ouvert — 2 emplacements dupliqués inchangés (`AlertesListeComponent`, `AuditJournalComponent`) et le dashboard Gestionnaire, adoption écran par écran à poursuivre. |
| DD-EP17-05 | Premier composant modal du produit (`lt-confirm-dialog`, requis par US-125) sans aucun précédent de focus-trap/restitution du focus dans le code existant | Majeur | Design Architect | Avant tout dialogue modal en Production | Test d'accessibilité dédié (`CHECK-ACCESSIBILITY-01`) | **Partiellement traité (2026-08-02)** — test dédié exécuté en navigateur réel (`US-132`, Chrome Headless) couvrant 5 des 6 exigences `DDS-LT-005` (focus-trap, restitution du focus — écart réel détecté et corrigé, `Échap`, rôle ARIA, libellés) ; exigence 6 (message post-action) couverte structurellement par `lt-toast`, non câblée à un appelant réel. Reste Ouvert — aucun dialogue modal encore en Production ni intégré à un écran métier (Lot 3+). |
| DD-EP17-06 | Valeurs de spacing non normalisées (échelle observée `0.35rem`→`1.5rem`, aucune convention de tokens) | Mineur | Design Architect | Lot 1 du Plan d'Exécution | Tokens `--lt-space-*` définis et adoptés | **Partiellement traité (2026-08-01)** — tokens `--lt-space-*` **définis** (US-129/DDS-LT-006, `_lt-tokens.scss`) ; **non adoptés** : les valeurs de spacing déjà en dur dans `styles.scss` (`0.4rem`/`0.8rem`/`0.6rem`/`0.9rem`/`0.75rem`) ne correspondent à aucun palier de l'échelle et ont été délibérément laissées littérales par US-131 pour ne pas introduire de régression visuelle. Reste Ouvert — l'adoption réelle relève de la migration écran par écran (Lots 2+). |
| DD-EP17-07 | Aucun sélecteur de test (`data-testid` ou équivalent) dans les composants existants | Mineur | Frontend Architect | Si des tests end-to-end sont introduits (non planifiés à ce jour) | Convention de nommage définie et appliquée | Ouvert |
| DD-EP17-08 | Aucune stratégie d'état **formalisée ni justifiée** : les services (`profil.service.ts`, `s02/s03/s04-api.service.ts`, etc.) exposent des `Observable` consommés directement par des champs de composant ; huit des onze composants existants utilisent en réalité déjà `signal()` — correction du constat initial (« sans signal ni store »), inexact, vérifié par `grep -rl "signal("` (`frontend-architecture.md` §State Management non instancié ; `ADR-UI-001` ne couvrait pas le sujet) | Majeur | Frontend Architect | Avant Lot 2 du Plan d'Exécution (composants transverses `lt-data-table`/`lt-form-field` devant gérer un état propre) | Stratégie d'état documentée (local/partagé/serveur, cache, invalidation, erreurs, concurrence) dans `ADR-UI-001` ou une DDS dédiée | **Close — acceptée par le Product Owner (jptshilombo@gmail.com), 2026-07-31, sans réserve** (`ADR-UI-001` §Stratégie d'état) : sept dimensions couvertes (local/serveur/partagé/cache/invalidation/erreurs/concurrence), fondées sur le code réel, aucune nouvelle dépendance ; bloqueur Gate 04A levé (`gate-04A-decision-ep17-lot0.md` §4) — ne rouvre pas à lui seul la décision Gate 04A (`DD-611-03` reste ouvert) |
| DD-EP17-09 | `VerifyReceiptComponent` (surface publique de vérification de quittance, EP-14) n'utilise ni le breakpoint ni l'échelle de radius du reste du produit : `@media (max-width: 560px)` contre `640px` partout ailleurs ; `border-radius` observés 2px/8px/10px/16px/50%/999px contre `6px` constant sur les 8 autres composants à patron `panel` — non identifié par l'inventaire initial (`component-inventory-loyertracker.md` indiquait à tort « Aucune dette identifiée à ce stade »), corrigé par la validation Design Architect du 2026-07-31 (`DD-611-02`) | Mineur | Design Architect | Avant adaptation visuelle de `VerifyReceiptComponent` (déjà noté « en dernier » dans l'inventaire, surface publique sensible) | Tokens `--lt-radius-*`/breakpoint harmonisés, ou exception documentée et justifiée (ex. contrainte d'impression/QR code) | Ouvert — identifié par la validation Design Architect (2026-07-31, cf. `DSG-001.md` §Avis de validation) |
| DD-EP17-10 | Absence d'état d'erreur explicite au chargement des listes Biens/Patrimoines (`dashboard.component.ts`, Bailleur) : `chargerReferentielsBien()` ne gère aucun cas d'échec réseau/serveur visible à l'écran — identifié par lecture directe du code lors de la production de `phase-02-ui-mockups-ep17-lot3.md` (EP-17 Lot 3) | Mineur | Frontend Architect | Lot 3 du Plan d'Exécution (introduction proposée avec la migration `lt-empty-state`) | État d'erreur `lt-empty-state` (variante erreur) affiché au chargement, testé | **Traité pour Biens et Patrimoines (2026-08-02)** — écart réel signalé plutôt que corrigé silencieusement : le mécanisme livré n'est **pas** `lt-empty-state` (variante erreur) comme anticipé, mais le paragraphe `role="alert"` déjà natif à `lt-data-table` depuis `US-132` (`error()`, sémantique ARIA plus forte que `role="status"`) — jugé équivalent, pas réécrit pour coller à la cible initiale. Câblé et testé (navigateur réel) pour **Biens** (`biensErreur`, `chargerBiens()`) puis **Patrimoines** (`patrimoinesErreur`, `chargerReferentielsBien()` — la méthode elle-même nommée dans le constat initial de cette dette). Reste Ouvert au sens strict de la preuve attendue (`lt-empty-state` non utilisé) — mécanisme équivalent jugé suffisant, décision non revue par un Design Architect distinct. |
| DD-EP17-11 | Touch targets `button` (composant global, `_button.scss`) sous la cible `≥ 44×44px` de `DSG-001.md` §Responsive Rules : `padding: 0.4rem 0.8rem` mesuré à 35px de hauteur en navigateur réel (Chrome Headless, police par défaut) — identifié en produisant la preuve `CHECK-UX-01` « Responsive » du Gate 04A Lot 3 (2026-08-02). `input`/`select` du dashboard Bailleur corrigés à cette occasion (`min-height: 44px`, périmètre Lot 3 restreint) ; `button` est un composant **global** (tous écrans du produit), correction hors périmètre de ce Lot — le commentaire de `_button.scss` avertit déjà explicitement contre toute modification visuelle non revue de ce fichier | Mineur | Design Architect | Avant tout Lot touchant `button` globalement, ou Lot de remédiation dédié à l'accessibilité tactile | Mesure en navigateur réel confirmant `≥ 44×44px`, ou exception documentée (ex. bouton secondaire dense, densité d'information prioritaire) | Ouvert — identifié 2026-08-02, non corrigé (hors périmètre Lot 3 restreint, blast radius produit entier) |
| DD-EP17-12 | Aucune interface (Angular ou Keycloak) pour l'acceptation d'invitation Gestionnaire : le lien généré par `InvitationService.java` (`{baseUrl}/invitations/{token}`) ne correspond à aucune route Angular (`app.routes.ts`, recherche exhaustive « invitation », 0 occurrence) ; le flux n'est exercé qu'en appel API direct (`POST /api/invitations/{token}/acceptation`, `infra/smoke/smoke-stack.sh`), fonctionnel et testé en Production mais jamais via une page web — identifié en vérifiant le périmètre réel des écrans du Gate 04A Lot 4 (Pilote Keycloak), qui supposait à tort que « invitation »/« invitation expirée » étaient des écrans Keycloak (0 occurrence de « invitation » dans les 2 fichiers de realm) | Majeur | Frontend Architect | Avant toute Story exposant ce lien à un utilisateur final hors contexte technique/API (ex. envoi réel de l'e-mail d'invitation à un Gestionnaire) | Écran Angular dédié (ou décision explicite d'exemption si le flux reste réservé à un usage technique) | Ouvert — identifié 2026-08-02, hors périmètre du Lot 4 (aucun écran Keycloak concerné) |

Ce registre ne rejoue pas les Gates historiques. Une exemption n'est recevable que pour un lot
strictement sans impact UI, avec justification dans le Project State. Aucune dette ci-dessus n'est
close par ce cadrage documentaire — seules les preuves déjà produites sont créditées, conformément
au Validation Framework CGPA v6.1.1 (§5, une preuve non produite est `non exécutée`, jamais
supposée).
